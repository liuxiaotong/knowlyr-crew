# ── Crew Webhook 服务器 ──

# Bearer token 认证（必填，生产环境务必设置）
KNOWLYR_CREW_API_TOKEN=your-secret-token

# CORS 允许的来源（逗号分隔，前端跨域调用时设置）
CREW_CORS_ORIGINS=https://antgather.knowlyr.com,http://localhost:3000

# ── LLM API Keys（按需设置，至少一个） ──

# Anthropic（claude-* 模型）
ANTHROPIC_API_KEY=sk-ant-xxx

# OpenAI（gpt-*/o1-*/o3-*/o4-* 模型）
OPENAI_API_KEY=sk-xxx

# DeepSeek（deepseek-* 模型，使用 OpenAI 兼容 API）
DEEPSEEK_API_KEY=sk-xxx

# Moonshot / Kimi（moonshot-* 模型，使用 OpenAI 兼容 API）
MOONSHOT_API_KEY=sk-xxx

# ── knowlyr-id 集成（可选） ──

# knowlyr-id 服务地址
KNOWLYR_ID_URL=https://id.knowlyr.com

# Agent API Token
AGENT_API_TOKEN=your-agent-token

# ── 蚁聚社区 internal API（消息发送已迁移至蚁聚） ──

# 蚁聚后端地址（配置后消息走蚁聚，未配置则 fallback 到 knowlyr-id）
ANTGATHER_API_URL=http://localhost:8200
# 蚁聚 internal API 服务间 token（对应蚁聚后端的 INTERNAL_SERVICE_TOKEN）
ANTGATHER_API_TOKEN=your-antgather-service-token

# ── Wiki 文件 API（可选） ──

# Wiki API 地址（蚁聚后端）
WIKI_API_URL=https://antgather.knowlyr.com
# Wiki API Token（X-Wiki-Token 鉴权）
WIKI_API_TOKEN=your-wiki-token

# ── 头像生成（可选） ──

# 通义万相 DashScope API Key
DASHSCOPE_API_KEY=sk-xxx

# ── 飞书 Bot（可选） ──

# 飞书应用凭证
FEISHU_APP_ID=cli_xxx
FEISHU_APP_SECRET=xxx

# 事件回调验证 Token（开放平台 → 事件与回调 → 加密策略）
FEISHU_VERIFICATION_TOKEN=xxx

# 日历 ID（日程相关功能需要）
# FEISHU_CALENDAR_ID=xxx

# ── 姜墨言专属 API（可选） ──

# aiberm 主模型
AIBERM_API_KEY=sk-xxx

# Kimi 备用模型
KIMI_API_KEY=sk-xxx

# ── GitHub（可选） ──

GITHUB_TOKEN=ghp_xxx

# ── 全局配置（可选） ──

# 全局员工目录（默认 ~/.knowlyr/crew/）
# KNOWLYR_CREW_GLOBAL_DIR=~/.knowlyr/crew/
